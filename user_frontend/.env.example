# React environment variables (Create React App)
# Note: variables MUST be prefixed with REACT_APP_ to be embedded at build time.

# API Gateway base URL (auth/register/login/refresh/logout + health)
# Used by src/api/gatewayClient.js
REACT_APP_API_BASE=http://localhost:8080

# Application Server base URL (protected endpoints like /me and /home)
# Used by src/api/appServerClient.js
REACT_APP_BACKEND_URL=http://localhost:8081

# Optional / legacy fallbacks (supported but not required)
# REACT_APP_GATEWAY_BASE_URL=http://localhost:8080
# REACT_APP_APP_SERVER_BASE_URL=http://localhost:8081

# Frontend base URL (used by some deployments and redirect configs if needed)
REACT_APP_FRONTEND_URL=http://localhost:3000

# Other container env vars may exist but are not required for the JWT flow.
