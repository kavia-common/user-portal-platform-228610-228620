# React environment variables (Create React App)
# Note: variables MUST be prefixed with REACT_APP_ to be embedded at build time.

# Normalized equivalents for:
# - GATEWAY_BASE_URL
# - APP_SERVER_BASE_URL
REACT_APP_API_BASE=http://localhost:3010
REACT_APP_BACKEND_URL=http://localhost:3001

# Optional: frontend origin/base URL (sometimes used for redirects or display)
REACT_APP_FRONTEND_URL=http://localhost:3000

# Optional / legacy fallbacks (supported but not required)
# REACT_APP_GATEWAY_BASE_URL=http://localhost:3010
# REACT_APP_APP_SERVER_BASE_URL=http://localhost:3001
