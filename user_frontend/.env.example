# React frontend environment variables
# IMPORTANT: CRA only exposes env vars prefixed with REACT_APP_

# API Gateway (JWT auth, health)
REACT_APP_GATEWAY_BASE_URL=http://localhost:3010

# Application Server (protected business endpoints)
REACT_APP_APP_SERVER_BASE_URL=http://localhost:3001
