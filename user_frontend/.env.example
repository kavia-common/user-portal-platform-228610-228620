# React environment variables (client-side)
# Note: CRA requires variables to be prefixed with REACT_APP_

# API Gateway base URL (auth endpoints + health)
# Used by: src/api/gatewayClient.js
REACT_APP_API_BASE=http://localhost:8000

# Application Server base URL (protected endpoints requiring Bearer JWT)
# Used by: src/api/appServerClient.js
REACT_APP_BACKEND_URL=http://localhost:8001

# Optional (legacy compatibility):
# REACT_APP_GATEWAY_BASE_URL=http://localhost:8000
# REACT_APP_APP_SERVER_BASE_URL=http://localhost:8001

# Optional UI/runtime config (provided by container env list; include as needed)
# REACT_APP_FRONTEND_URL=http://localhost:3000
# REACT_APP_WS_URL=ws://localhost:8000
# REACT_APP_NODE_ENV=development
# REACT_APP_LOG_LEVEL=info
